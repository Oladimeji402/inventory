# Leave blank to run fully client-side (data lives in the browser's localStorage,
# seeded from src/data/seed.json).
#
# Once your backend is ready, set this to its base URL and the app will call
# real REST endpoints instead — no other code changes required. See
# src/services/store.js for the exact request shape expected per resource
# (/products, /employees, /sales, /audit-log).
VITE_API_BASE_URL=
